#! /bin/bash
sudo yum update -y
sudo yum install -y docker
sudo service docker start
sudo usermod -a -G docker ec2-user
sudo docker pull ${image}
sudo docker run --name app -e "SPRING_PROFILES_ACTIVE=${active_profiles}" -p 80:8080 -d ${image}
